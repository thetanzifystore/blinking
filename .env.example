# Blinking - .env.example
# Copy this file to `.env.local` and fill in real values before running locally.

# --------------------
# Client (NEXT_PUBLIC_*) - safe to expose to browser
# --------------------
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# --------------------
# Server (sensitive) - do NOT commit these to VCS
# --------------------
# Provide server-side Firebase service account credentials either as a JSON
# string (FIREBASE_SERVICE_ACCOUNT) or as a path to a JSON file
# (FIREBASE_SERVICE_ACCOUNT_PATH). Use one method only.
# Option A: Inline JSON (useful for CI secrets)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n","client_email":"..."}
# Option B: Local path to service account JSON (for dev)
FIREBASE_SERVICE_ACCOUNT_PATH=./secrets/firebase-service-account.json

# Optional server overrides (if different from NEXT_PUBLIC_* values)
FIREBASE_STORAGE_BUCKET=

# --------------------
# Redis (used by admin rate limiter)
# --------------------
# Example: redis://127.0.0.1:6379
REDIS_URL=redis://127.0.0.1:6379

# --------------------
# Emulator/Test helpers (uncomment when using emulators)
# --------------------
# FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
# STORAGE_EMULATOR_HOST=127.0.0.1:9199
# EMULATOR_HOST=127.0.0.1

# --------------------
# Notes
# --------------------
# - Copy this file to .env.local and replace placeholder values.
# - For CI, prefer setting FIREBASE_SERVICE_ACCOUNT as a single-line secret.
# - Keep service account JSON files outside version control.
# - If you commit package-lock.json, ensure all collaborators use the same npm/node versions.
